```

Este es un archivo vacío que le indica a GitHub que no procese tu sitio con Jekyll.

## 5. Configura el deploy en tu package.json:

```json
{
  "name": "medicine-calendar",
  "homepage": "https://juanmartin19l.github.io/medicine-calendar/",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}